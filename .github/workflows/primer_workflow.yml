name: CI Simple
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v3
      - name: Ejecutar script en github
        run: |
          echo "Este es un script shell de varias líneas."
          echo "El sistema operativo es $RUNNER_OS"
          echo "La fecha es: $(date)"
          echo "Estamos en la carpeta: $(pwd)"
